#include <iostream>
#include <string>

namespace TokenizerRegex {
    std::string
        assignOp = R"([+\-*/%&|^]?=|<<=|>>=)",
        arithOp = R"([+\-*/%&|^~]|>>|<<)",
        logicOp = R"(!|&&|\|\|)",
        compareOp = R"([<>!=]=|[<>])"
        
        
    
}
